/**
 * Dependency-free Memory Console.
 *
 * The DSH settings panel is the visual reference for this page: pale canvas,
 * white rounded cards, compact black controls, and quiet status colours. This
 * copy deliberately talks to the Gateway HTTP API only; it never opens the
 * SQLite file from the browser.
 */
export const MEMORY_CONSOLE_HTML = `<!doctype html>
<html lang="zh-CN">
<head>
<meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>StrataGate Memory Console</title>
<style>
:root{color-scheme:light;--bg:#f6f6f5;--surface:#fff;--ink:#171717;--muted:#8b8b8b;--line:#e9e9e7;--soft:#f1f1ef;--ok:#16a879;--warn:#ef704d;--shadow:0 12px 36px rgba(20,20,20,.055)}
*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--ink);font:14px/1.55 Inter,ui-sans-serif,system-ui,-apple-system,"PingFang SC","Microsoft YaHei",sans-serif}button,input,select,textarea{font:inherit}button{cursor:pointer;border:0;background:none;color:inherit}.app{max-width:1420px;margin:auto;padding:24px 28px 50px}.top{display:flex;align-items:center;justify-content:space-between;margin-bottom:26px}.brand{display:flex;gap:11px;align-items:center}.logo{display:grid;place-items:center;width:36px;height:36px;border-radius:11px;background:#111;color:#fff;font-size:20px;font-weight:800}.brand h1{margin:0;font-size:16px;letter-spacing:-.03em}.brand small{display:block;color:#aaa;font-size:10px;letter-spacing:.14em}.nav{display:flex;gap:22px;color:#777;font-size:13px}.nav button.active{color:#111;font-weight:700}.layout{display:grid;grid-template-columns:208px minmax(0,1fr);gap:24px}.side{height:max-content;padding:9px;border:1px solid var(--line);border-radius:18px;background:var(--surface);box-shadow:var(--shadow);position:sticky;top:18px}.side button{display:flex;align-items:center;gap:10px;width:100%;padding:12px 13px;border-radius:12px;text-align:left;color:#777}.side button:hover{background:#fafafa;color:#222}.side button.active{background:#111;color:#fff}.side .ico{width:18px;text-align:center;font-size:16px}.side-sep{height:1px;background:var(--line);margin:8px 5px}.content{min-width:0}.hero{display:flex;justify-content:space-between;align-items:flex-end;margin:2px 0 18px}.hero h2{margin:0;font-size:27px;letter-spacing:-.05em}.hero p{margin:5px 0 0;color:var(--muted)}.actions{display:flex;gap:8px}.btn{padding:9px 14px;border:1px solid var(--line);border-radius:10px;background:var(--surface)}.btn:hover{border-color:#bdbdbd}.btn.primary{background:#111;border-color:#111;color:#fff}.btn:disabled{cursor:default;opacity:.45}.namespace{display:flex;align-items:center;gap:10px;margin:0 0 16px}.namespace-label{color:var(--muted);font-size:12px;white-space:nowrap}.namespace-select-wrap{position:relative;flex:1;min-width:0;max-width:560px}.ns-trigger{display:flex;align-items:center;justify-content:space-between;gap:10px;width:100%;padding:10px 14px;border:1px solid var(--line);border-radius:10px;background:var(--surface);color:var(--ink);text-align:left;box-shadow:0 2px 8px rgba(20,20,20,.025);transition:border-color .15s,box-shadow .15s}.ns-trigger:hover{border-color:#bdbdbd}.ns-trigger:focus-visible,.ns-wrap.open .ns-trigger{border-color:#999;box-shadow:0 0 0 3px rgba(17,17,17,.08)}.ns-trigger:disabled{cursor:default;opacity:.6}.ns-trigger-label{overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-weight:600}.ns-caret{flex:none;color:var(--muted);font-size:12px;transition:transform .18s}.ns-wrap.open .ns-caret{transform:rotate(180deg)}.ns-menu{position:absolute;top:calc(100% + 6px);left:0;right:0;z-index:30;display:none;max-height:320px;overflow:auto;padding:6px;border:1px solid var(--line);border-radius:13px;background:var(--surface);box-shadow:0 18px 48px rgba(20,20,20,.16)}.ns-wrap.open .ns-menu{display:block;animation:ns-pop .16s ease}@keyframes ns-pop{from{opacity:0;transform:translateY(-4px)}to{opacity:1;transform:none}}.ns-option{display:flex;align-items:center;gap:8px;width:100%;padding:9px 11px;border-radius:8px;text-align:left;color:var(--ink)}.ns-option:hover,.ns-option:focus{background:#f5f5f4;outline:none}.ns-option.active{background:#111;color:#fff}.ns-option.active:hover,.ns-option.active:focus{background:#000}.ns-check{width:15px;flex:none;text-align:center;font-size:12px}.ns-name{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.ns-empty{padding:14px;text-align:center;color:var(--muted);font-size:12px}.cards{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px;margin-bottom:14px}.card{padding:17px 19px;border:1px solid var(--line);border-radius:16px;background:var(--surface);box-shadow:var(--shadow)}.label{color:#aaa;font-size:11px;letter-spacing:.09em}.value{margin-top:10px;font-size:27px;font-weight:760;letter-spacing:-.04em}.sub{margin-top:3px;color:var(--muted);font-size:12px}.panel{margin-top:14px;padding:20px;border:1px solid var(--line);border-radius:18px;background:var(--surface);box-shadow:var(--shadow)}.panel-head{display:flex;justify-content:space-between;align-items:center;gap:12px;margin-bottom:14px}.panel-head h3{margin:0;font-size:18px;letter-spacing:-.03em}.muted{color:var(--muted)}.pill{padding:5px 9px;border-radius:999px;background:#e9faf4;color:var(--ok);font-size:11px;font-weight:700;white-space:nowrap}.pill.warn{background:#fff0e8;color:var(--warn)}.search{display:flex;gap:8px;margin-bottom:12px}.search input{width:100%;padding:10px 12px;border:1px solid var(--line);border-radius:10px;background:#fff;outline:none}.search input:focus{border-color:#999}.search button{padding:0 15px;border-radius:10px;background:#111;color:#fff}.rows{display:grid;gap:8px}.row{display:grid;grid-template-columns:minmax(0,1fr) repeat(3,minmax(72px,auto));gap:12px;align-items:center;padding:13px 12px;border:1px solid var(--line);border-radius:11px}.row.clickable{cursor:pointer}.row.clickable:hover{background:#fafafa;border-color:#d4d4d2}.row-title{font-weight:650;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.row-meta{color:var(--muted);font-size:12px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.metric{text-align:right}.metric b{display:block;font-size:16px}.metric span{color:var(--muted);font-size:11px}.empty{padding:34px;text-align:center;color:var(--muted)}.grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.detail{display:grid;grid-template-columns:repeat(4,1fr);gap:9px}.mini{padding:12px;border-radius:11px;background:var(--soft)}.mini b{display:block;font-size:20px}.mini span{color:var(--muted);font-size:11px}.toolbar{display:flex;gap:8px;align-items:center;flex-wrap:wrap;margin-bottom:12px}.toolbar .search{flex:1;min-width:220px;margin:0}.tabs{display:flex;gap:4px;padding:4px;border-radius:11px;background:var(--soft);width:max-content}.tabs button{padding:7px 13px;border-radius:8px;color:var(--muted)}.tabs button.active{background:#111;color:#fff}.block{padding:15px;border:1px solid var(--line);border-radius:13px;background:#fff}.block+.block{margin-top:9px}.block-head{display:flex;align-items:center;justify-content:space-between;gap:10px}.block-title{font-weight:700}.block-meta{color:var(--muted);font-size:12px}.layers{display:grid;gap:6px;margin-top:12px}.layer{display:grid;grid-template-columns:40px minmax(0,1fr);border:1px solid var(--line);border-radius:8px;background:var(--soft);overflow:hidden}.layer b{display:grid;place-items:center;border-right:1px solid var(--line);font-size:12px}.layer pre{margin:0;padding:8px 10px;white-space:pre-wrap;overflow-wrap:anywhere;font:12px/1.5 ui-monospace,SFMono-Regular,Consolas,monospace;color:#555}.event-card{padding:14px;border:1px solid var(--line);border-radius:12px;background:#fff;cursor:pointer}.event-card:hover{border-color:#c8c8c8;background:#fdfdfc}.event-title{font-weight:700}.event-summary{margin:5px 0;color:#555}.tags{display:flex;gap:6px;flex-wrap:wrap}.tag{padding:3px 7px;border-radius:999px;background:var(--soft);color:#777;font-size:11px}.split{display:grid;grid-template-columns:minmax(0,1fr) minmax(280px,.7fr);gap:14px}.graph-list{display:grid;gap:8px;max-height:540px;overflow:auto}.graph-node{padding:11px 12px;border:1px solid var(--line);border-radius:10px;cursor:pointer}.graph-node:hover{border-color:#111}.graph-node strong{display:block}.graph-node span{color:var(--muted);font-size:12px}.json{margin:0;padding:13px;border-radius:10px;background:#181818;color:#e8e8e8;white-space:pre-wrap;overflow:auto;font:12px/1.5 ui-monospace,SFMono-Regular,Consolas,monospace;max-height:560px}.form-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.field{display:grid;gap:6px}.field label{color:#777;font-size:12px}.field input{width:100%;padding:10px 12px;border:1px solid var(--line);border-radius:10px;background:#fff}.notice{padding:11px 13px;border-radius:10px;background:#fff8e8;color:#9b6b18}.footer{margin-top:22px;color:#aaa;font-size:11px;text-align:center}.error{padding:12px 14px;border:1px solid #ffd5c7;border-radius:10px;background:#fff5f1;color:#bd4f31}.modal{position:fixed;inset:0;z-index:10;display:grid;place-items:center;padding:18px;background:rgba(0,0,0,.32)}.dialog{width:min(820px,calc(100vw - 26px));max-height:calc(100vh - 36px);overflow:auto;padding:20px;border:1px solid var(--line);border-radius:18px;background:#fff;box-shadow:0 24px 80px rgba(0,0,0,.24)}.dialog-head{display:flex;align-items:flex-start;justify-content:space-between;gap:12px}.dialog h3{margin:0;font-size:20px}.close{width:30px;height:30px;border-radius:8px;background:var(--soft);font-size:20px}.source{display:grid;gap:8px;margin-top:14px}.message{padding:10px 12px;border-left:3px solid #d8d8d5;background:var(--soft);white-space:pre-wrap;overflow-wrap:anywhere}.message b{display:block;margin-bottom:3px;color:#888;font-size:11px;text-transform:uppercase}.activity{display:grid;grid-template-columns:145px minmax(0,1fr) auto;gap:12px;align-items:center;padding:11px 0;border-bottom:1px solid var(--line)}.activity:last-child{border-bottom:0}.activity time{color:var(--muted);font-size:12px}.activity strong{font-size:13px}.activity span{color:var(--muted);font-size:12px}
@media(max-width:980px){.layout{grid-template-columns:1fr}.side{position:static;display:flex;overflow:auto;gap:4px}.side button{min-width:max-content}.side-sep{width:1px;height:32px;margin:0 4px}.cards{grid-template-columns:repeat(2,1fr)}.split{grid-template-columns:1fr}}
@media(max-width:620px){.app{padding:16px}.top{margin-bottom:18px}.nav{display:none}.hero{align-items:flex-start;gap:12px;flex-direction:column}.cards,.grid,.form-grid{grid-template-columns:1fr}.row{grid-template-columns:1fr 1fr}.metric{text-align:left}.detail{grid-template-columns:1fr 1fr}.activity{grid-template-columns:1fr;gap:3px}}
</style><style>.filters{display:flex;gap:8px;align-items:center;flex-wrap:wrap}.filter{display:flex;align-items:center;gap:6px;color:var(--muted);font-size:12px;white-space:nowrap}.filter select{min-width:118px;padding:9px 26px 9px 10px;border:1px solid var(--line);border-radius:10px;background:var(--surface);color:var(--ink);outline:none}.filter select:focus{border-color:#999}.row-meta:has(.source-badge){display:flex;align-items:center;gap:8px}.source-badge{display:inline-flex;align-items:center;gap:5px;flex:none;color:#d33b3b;font-size:13px;font-weight:750;letter-spacing:.01em}.source-badge::before{content:'●';font-size:9px}.row-agent,.row-preview{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}@media(max-width:620px){.filters{width:100%}.filter{flex:1}.filter select{width:100%}}.filter{position:relative}.filter select{-webkit-appearance:none;appearance:none;background-color:var(--surface);background-image:none;padding-right:31px;cursor:pointer;transition:border-color .15s,box-shadow .15s}.filter select:hover{border-color:#bdbdbd}.filter select:focus-visible{border-color:#999;box-shadow:0 0 0 3px rgba(17,17,17,.08);outline:none}.filter select:disabled{cursor:default;opacity:.6}.filter::after{content:'▾';position:absolute;right:10px;top:50%;transform:translateY(12%);color:var(--muted);font-size:12px;line-height:1;pointer-events:none}.ns-trigger{-webkit-appearance:none;appearance:none}.mini.source-mini b,.mini.source-mini span{color:#d33b3b}.mini.source-mini span{font-weight:700}</style></head>
<body><div id="app"></div>
<script>
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const text=v=>String(v??'').replace(/\\s+/g,' ').trim();
const fmt=v=>{if(!v)return'时间未知';const d=new Date(v);return Number.isNaN(d.getTime())?String(v):new Intl.DateTimeFormat('zh-CN',{timeZone:'Asia/Shanghai',year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',hour12:false}).format(d)};
const kindName={events:'Event',elements:'Element',graph:'Graph',blocks:'Block',audit:'使用审计',raw:'原始消息'};
let state={dashboard:null,snapshot:null,namespace:'',nsOpen:false,view:'overview',subview:'events',query:'',agentFilter:'',sourceFilter:'',selected:null,error:'',loading:false,authenticated:null,loggingIn:false};
async function api(path,options={}){const token=localStorage.getItem('stratagate_gateway_token')||'';const headers=Object.assign({},token?{'Authorization':'Bearer '+token}:{},options.headers||{});const res=await fetch(path,Object.assign({},options,{cache:'no-store',headers}));const data=await res.json().catch(()=>({}));if(res.status===401){state.authenticated=false;state.error='';throw new Error('Memory Gateway authorization required')}if(!res.ok)throw new Error(data.error||'HTTP '+res.status);state.authenticated=true;return data}
function loginPage(){return'<div style="min-height:100vh;display:grid;place-items:center;padding:24px"><section style="width:min(430px,100%);padding:30px;border:1px solid #e9e9e7;border-radius:22px;background:#fff;box-shadow:0 24px 70px rgba(20,20,20,.09)"><div style="display:flex;gap:11px;align-items:center;margin-bottom:28px"><div class="logo">✦</div><div><h1 style="margin:0;font-size:16px">StrataGate</h1><small style="color:#aaa;letter-spacing:.14em">MEMORY CONSOLE</small></div></div><h2 style="margin:0;font-size:25px;letter-spacing:-.05em">登录记忆控制台</h2><p style="margin:7px 0 22px;color:#8b8b8b">输入 Gateway Token，查看当前工作区的可追溯记忆。</p><form id="login-form" style="display:grid;gap:10px"><label for="token" style="color:#777;font-size:12px">Gateway Token</label><input id="token" name="token" type="password" autocomplete="off" placeholder="粘贴 STRATAGATE_GATEWAY_TOKEN" required style="width:100%;padding:12px 13px;border:1px solid #e9e9e7;border-radius:10px;background:#fff;outline:none"><button type="submit" '+(state.loggingIn?'disabled':'')+' style="margin-top:5px;padding:11px 14px;border:0;border-radius:10px;background:#111;color:#fff">'+(state.loggingIn?'正在验证…':'使用 Token 登录')+'</button></form>'+(state.error?'<div class="error" style="margin-top:14px">'+esc(state.error)+'</div>':'')+'<p style="margin:16px 0 0;color:#8b8b8b;font-size:12px">Token 只保存在当前浏览器的本地存储中。</p></section></div>'}
function showLogin(){const root=document.getElementById('app');if(!root)return;root.innerHTML=loginPage();const form=document.getElementById('login-form');form?.addEventListener('submit',event=>{event.preventDefault();void login(new FormData(form).get('token'))});document.getElementById('token')?.focus()}
async function login(token){const supplied=String(token||'').trim();if(!supplied)return;state.loggingIn=true;state.error='';render();try{const res=await fetch('/health',{cache:'no-store',headers:{Authorization:'Bearer '+supplied}});const data=await res.json().catch(()=>({}));if(!res.ok)throw new Error(data.error||'Token 无效（HTTP '+res.status+'）');localStorage.setItem('stratagate_gateway_token',supplied);state.authenticated=true;state.loggingIn=false;await loadDashboard()}catch(e){state.loggingIn=false;state.authenticated=false;state.error=e.message||String(e);render()}}
async function loadDashboard(){state.loading=true;state.error='';render();try{state.dashboard=await api('/v1/dashboard');const rows=state.dashboard.namespaces||[];if(!state.namespace||!rows.some(x=>x.namespace===state.namespace))state.namespace=rows[0]?.namespace||'';if(state.namespace)await loadSnapshot()}catch(e){state.error=e.message||String(e)}finally{state.loading=false;render()}}
async function loadSnapshot(){if(!state.namespace){state.snapshot=null;return}try{state.snapshot=await api('/v1/console/snapshot?namespace='+encodeURIComponent(state.namespace))}catch(e){state.snapshot=null;state.error=e.message||String(e)}}
async function refresh(){await loadDashboard()}
function selectedRow(){return(state.dashboard?.namespaces||[]).find(x=>x.namespace===state.namespace)||null}
function messages(){const s=state.snapshot||{};return[...(s.openTail||[]),...(s.blocks||[]).flatMap(b=>b.l5Raw||[])].sort((a,b)=>String(a.createdAt).localeCompare(String(b.createdAt)))}
function threadRows(){const map=new Map();for(const m of messages()){const id=m.threadId||'__legacy__';const row=map.get(id)||{id,messages:[],last:null};row.messages.push(m);row.last=m.createdAt;map.set(id,row)}return[...map.values()].sort((a,b)=>String(b.last).localeCompare(String(a.last)))}
function layerRows(block){return[{level:0,content:block.l0Title?block.l0Title+'\\n标签：'+(block.l0Tags||[]).join('、'):'(尚未生成)'},{level:1,content:block.l1Summary||'(尚未生成)'},{level:2,content:(block.l2Keypoints||[]).map(x=>'• '+x).join('\\n')||'(尚未生成)'},{level:3,content:block.l3Condensed||'(尚未生成)'},{level:4,content:block.l4Readable||'(尚未生成)'},{level:5,content:(block.l5Raw||[]).map(m=>m.role+': '+m.content).join('\\n\\n')}];}
function card(label,value,sub){return'<div class="card"><div class="label">'+esc(label)+'</div><div class="value">'+esc(value)+'</div><div class="sub">'+esc(sub)+'</div></div>'}
function header(title,copy){return'<div class="hero"><div><h2>'+esc(title)+'</h2><p>'+esc(copy)+'</p></div><div class="actions"><button class="btn" data-action="refresh">↻ 刷新</button></div></div>'}
function overview(){const rows=state.dashboard?.namespaces||[], r=selectedRow(), all=rows.reduce((a,x)=>({turns:a.turns+(x.turns||0),blocks:a.blocks+(x.blocks||0),events:a.events+(x.events||0),elements:a.elements+(x.elements||0)}),{turns:0,blocks:0,events:0,elements:0});const s=state.snapshot||{};const acts=[...(s.blocks||[]).map(x=>({time:x.createdAt,title:'Block '+x.sequence,copy:x.l0Title||'分层内容'})),...(s.events||[]).map(x=>({time:x.updatedAt,title:'Event '+x.title,copy:x.summary||''})),...(s.usageReceipts||[]).map(x=>({time:x.createdAt,title:'使用审计',copy:(x.eventIds||[]).length+' 条 Event · '+(x.elementIds||[]).length+' 条 Element'}))].sort((a,b)=>String(b.time).localeCompare(String(a.time))).slice(0,8);const current=rows.find(x=>x.namespace===state.namespace)||null;const options=rows.map(x=>'<button type="button" class="ns-option'+(x.namespace===state.namespace?' active':'')+'" role="option" aria-selected="'+(x.namespace===state.namespace)+'" data-ns="'+esc(x.namespace)+'"><span class="ns-check">'+(x.namespace===state.namespace?'✓':'')+'</span><span class="ns-name">'+esc(x.label||x.namespace)+'</span></button>').join('');return header('记忆总览','统一查看来自 DSH、WorkBuddy、Codex 与 ZCode 的可追溯记忆')+'<div class="namespace"><label class="namespace-label" id="ns-label">项目</label><div class="namespace-select-wrap ns-wrap'+(state.nsOpen?' open':'')+'" id="ns-wrap"><button type="button" id="ns-trigger" class="ns-trigger" aria-haspopup="listbox" aria-expanded="'+(state.nsOpen?'true':'false')+'" aria-labelledby="ns-label"'+(rows.length?'':' disabled')+'><span class="ns-trigger-label">'+esc(current?.label||current?.namespace||'选择项目')+'</span><span class="ns-caret">▾</span></button><div class="ns-menu" role="listbox" aria-labelledby="ns-label">'+(options||'<div class="ns-empty">暂无命名空间</div>')+'</div></div></div>'+'<section class="cards">'+card('对话轮次',all.turns,'所有命名空间')+card('记忆 Blocks',all.blocks,'L0–L5 分层')+card('Events',all.events,'证据门控')+card('Elements',all.elements,'当前事实')+card('接入来源',rows.length,'个命名空间')+card('Open Tail',r?.openTailMessages||0,'未封存消息')+card('处理队列',r?.processingJobs||0,'后台任务')+card('Graph Nodes',r?.graphNodes||0,'知识图谱节点')+card('服务状态',state.dashboard?.status==='ok'?'正常':'检查中','Gateway')+'</section>'+'<section class="panel"><div class="panel-head"><h3>当前命名空间</h3><span class="pill">'+(r?'已连接':'等待数据')+'</span></div><div class="detail">'+[['用户',r?.userId||'default'],['Agent',r?.agents?.join('、')||'—'],['来源',r?.sourceAdapters?.join('、')||'—'],['Revision',r?.revision||0]].map(x=>'<div class="mini '+(x[0]==='来源'?'source-mini':'')+'"><b>'+esc(x[1])+'</b><span>'+esc(x[0])+'</span></div>').join('')+'</div></section>'+'<section class="panel"><div class="panel-head"><h3>最近活动</h3><span class="muted">只读 · 所有写入经过 Gateway</span></div>'+(acts.length?acts.map(a=>'<div class="activity"><time>'+esc(fmt(a.time))+'</time><strong>'+esc(a.title)+'</strong><span>'+esc(a.copy)+'</span></div>').join(''):'<div class="empty">暂无活动</div>')+'</section>'}
function processing(){const s=state.snapshot||{}, groups=[['摘要任务',s.summaryJobs||[]],['Event 提取',s.extractionJobs||[]],['Element 投影',s.elementProjectionJobs||[]],['Graph 投影',s.graphProjectionJobs||[]]];const body=groups.map(g=>{const rows=g[1].length?g[1].map(j=>'<div class="activity"><time>'+esc(fmt(j.updatedAt))+'</time><strong>'+esc(j.status||'unknown')+'</strong><span>'+esc(j.lastError||j.blockId||j.id||'—')+'</span></div>').join(''):'<div class="empty">暂无任务</div>';return'<div class="panel-head" style="margin-top:8px"><h3>'+g[0]+'</h3><span class="muted">'+g[1].length+' 个</span></div>'+rows}).join('');return header('处理状态','显示后台任务、最后错误与当前处理阶段')+'<section class="panel">'+body+'</section>'}
function conversations(){const rows=threadRows(), q=state.query.toLowerCase(), agentFilter=state.agentFilter||'', sourceFilter=state.sourceFilter||'';const agents=[...new Set(rows.flatMap(r=>r.messages.map(m=>String(m.agentId||'').trim()).filter(Boolean)))].sort();const adapters=[...new Set(rows.flatMap(r=>r.messages.map(m=>String(m.sourceAdapter||'').trim()).filter(Boolean)))].sort();const filtered=rows.filter(r=>{const hay=!q||JSON.stringify(r).toLowerCase().includes(q);const rowAgents=r.messages.map(m=>String(m.agentId||'').trim());const rowAdapters=r.messages.map(m=>String(m.sourceAdapter||'').trim());return hay&&(!agentFilter||rowAgents.includes(agentFilter))&&(!sourceFilter||rowAdapters.includes(sourceFilter))});const count=filtered.length===rows.length?rows.length+' 个对话':filtered.length+' / '+rows.length+' 个对话';const options=(values,selected,allLabel,id)=>'<label class="filter"><span>'+allLabel+'</span><select id="'+id+'" aria-label="'+allLabel+'筛选"><option value="">全部</option>'+values.map(value=>'<option value="'+esc(value)+'"'+(selected===value?' selected':'')+'>'+esc(value)+'</option>').join('')+'</select></label>';return header('对话来源','按 Agent、Conversation 和 Thread 浏览原始消息')+'<div class="toolbar"><div class="search"><input id="query" value="'+esc(state.query)+'" placeholder="搜索对话或消息内容"><button data-action="search">搜索</button></div><div class="filters">'+options(agents,agentFilter,'Agent','agent-filter')+options(adapters,sourceFilter,'来源','source-filter')+'</div><span class="muted">'+count+'</span></div><section class="panel"><div class="rows">'+(filtered.map(r=>{const rowAgents=[...new Set(r.messages.map(m=>m.agentId).filter(Boolean))].join('、')||'—';const rowAdapters=[...new Set(r.messages.map(m=>m.sourceAdapter).filter(Boolean))].join('、')||'未标注';return'<div class="row clickable" data-thread="'+esc(r.id)+'"><div><div class="row-title">'+esc(r.id==='__legacy__'?'历史对话':r.id)+'</div><div class="row-meta"><span class="source-badge">来源：'+esc(rowAdapters)+'</span><span class="row-agent">Agent：'+esc(rowAgents)+'</span><span class="row-preview"> · '+esc(text(r.messages.find(m=>m.role==='user')?.content)||'暂无用户消息')+'</span></div></div><div class="metric"><b>'+r.messages.length+'</b><span>消息</span></div><div class="metric"><b>'+r.messages.filter(m=>m.role==='user').length+'</b><span>轮次</span></div><div class="metric"><b>'+esc(fmt(r.last))+'</b><span>最近活动</span></div></div>'}).join('')||'<div class="empty">暂无匹配的对话</div>')+'</div></section>'}
function blocks(){const s=state.snapshot||{}, q=state.query.toLowerCase();const values=(s.blocks||[]).filter(b=>!q||JSON.stringify(b).toLowerCase().includes(q));return header('Block 分层','查看 L0–L5、处理状态和关联来源')+'<div class="toolbar"><div class="search"><input id="query" value="'+esc(state.query)+'" placeholder="搜索 Block 标题、摘要或消息"><button data-action="search">搜索</button></div><span class="muted">'+values.length+' / '+(s.blocks||[]).length+' 个 Block</span></div><section class="panel">'+(values.length?values.map(b=>{const related=(s.events||[]).filter(e=>e.sourceBlockId===b.id).map(e=>e.title);return'<article class="block"><div class="block-head"><div><div class="block-title">'+esc(b.l0Title||'Block #'+b.sequence)+'</div><div class="block-meta">Turn '+esc((b.startTurn||0)+'–'+(b.endTurn||0))+' · '+esc(b.processingStatus||'unknown')+' · '+esc(fmt(b.createdAt))+'</div></div><span class="pill '+(b.processingStatus==='ready'?'':'warn')+'">'+esc(b.processingStatus||'unknown')+'</span></div>'+(related.length?'<div class="row-meta" style="margin-top:8px">关联 Event：'+esc(related.join('、'))+'</div>':'')+'<div class="layers">'+layerRows(b).map(l=>'<div class="layer"><b>L'+l.level+'</b><pre>'+esc(l.content||'')+'</pre></div>').join('')+'</div></article>'}).join(''):'<div class="empty">暂无 Block</div>')+'</section>'}
function eventCard(e){const t=e.temporal||{};return'<article class="event-card" data-id="'+esc(e.id)+'" data-kind="event"><div class="event-title">'+esc(e.title||'未命名 Event')+'</div><div class="event-summary">'+esc(e.summary||e.narrative||'暂无摘要')+'</div><div class="tags"><span class="tag">'+esc(t.eventType||'其他')+'</span><span class="tag">'+esc(t.status||e.status||'未知')+'</span><span class="tag">'+esc(fmt(t.happenedStart||t.mentionedAt||e.updatedAt))+'</span></div></article>'}
function structure(){const s=state.snapshot||{}, sub=state.subview||'events';let body='';if(sub==='events'){const q=state.query.toLowerCase();const es=(s.events||[]).filter(e=>!q||JSON.stringify(e).toLowerCase().includes(q));body=es.length?'<div class="grid">'+es.map(eventCard).join('')+'</div>':'<div class="empty">暂无 Event</div>'}else if(sub==='elements'){const q=state.query.toLowerCase();const es=(s.elements||[]).filter(e=>!q||JSON.stringify(e).toLowerCase().includes(q));body=es.length?'<div class="rows">'+es.map(e=>'<div class="row clickable" data-id="'+esc(e.id)+'" data-kind="element"><div><div class="row-title">'+esc(e.name)+'</div><div class="row-meta">'+esc(e.type)+' · '+esc(e.currentState||'暂无当前状态')+'</div></div><div class="metric"><b>'+((e.facts||[]).length)+'</b><span>事实</span></div><div class="metric"><b>'+((e.sourceEventIds||[]).length)+'</b><span>来源 Event</span></div></div>').join('')+'</div>':'<div class="empty">暂无 Element</div>'}else{body='<div class="split"><div class="graph-list">'+((s.graphNodes||[]).map(n=>'<div class="graph-node" data-id="'+esc(n.id)+'" data-kind="graph"><strong>'+esc(n.name)+'</strong><span>'+esc(n.type)+' · '+((n.sourceEventIds||[]).length)+' 条支撑 Event</span></div>').join('')||'<div class="empty">暂无 Graph Node</div>')+'</div><pre class="json">'+esc(JSON.stringify({edges:s.graphEdges||[]},null,2))+'</pre></div>'}return header('记忆结构','Event、Element 与知识图谱的当前状态和来源链')+'<div class="toolbar"><div class="tabs">'+[['events','Events'],['elements','Elements'],['graph','Graph']].map(x=>'<button class="'+(sub===x[0]?'active':'')+'" data-subview="'+x[0]+'">'+x[1]+'</button>').join('')+'</div><div class="search"><input id="query" value="'+esc(state.query)+'" placeholder="筛选当前结构"><button data-action="search">搜索</button></div></div><section class="panel">'+body+'</section>'}
function audit(){const s=state.snapshot||{}, rows=[...(s.usageReceipts||[])].reverse();return header('使用审计','查看 retrieval batch、assessment 和 adoption receipt')+'<section class="panel"><div class="panel-head"><h3>采用回执</h3><span class="muted">'+rows.length+' 条</span></div>'+(rows.length?rows.map(r=>'<article class="block"><div class="block-head"><div><div class="block-title">使用了 '+((r.eventIds||[]).length+(r.elementIds||[]).length)+' 条记忆</div><div class="block-meta">'+esc(fmt(r.createdAt))+' · receipt '+esc(r.id)+'</div></div><span class="pill">已记录</span></div><div class="row-meta" style="margin-top:8px">Event：'+esc((r.eventIds||[]).join('、')||'无')+'<br>Element：'+esc((r.elementIds||[]).join('、')||'无')+'</div><pre class="json" style="margin-top:10px;max-height:180px">'+esc(JSON.stringify(r.audit||{},null,2))+'</pre></article>').join(''):'<div class="empty">还没有使用记录</div>')+'</section>'}
function settings(){const s=state.snapshot||{}, d=selectedRow();return header('设置','Block 大小、衰减参数、Gateway 地址和认证状态')+'<section class="panel"><div class="notice">第一版 Console 不直接编辑历史事实。设置修改仍由宿主 Runtime 或受控管理 API 完成。</div><div class="form-grid" style="margin-top:14px">'+[['Block 对话轮数',s.blockTurnSize??'—','每个 Block 的用户/助手轮次数'],['Block 衰减系数 λ',s.blockDecayLambda??'—','数值越小，记忆遗忘越慢'],['当前 namespace',state.namespace||'—','浏览器只通过 Gateway 读取'],['Gateway 状态',state.dashboard?.status==='ok'?'正常':'检查中','本地服务']].map(x=>'<div class="field"><label>'+esc(x[0])+'</label><input value="'+esc(x[1])+'" readonly><span class="muted">'+esc(x[2])+'</span></div>').join('')+'</div><div class="panel" style="margin:14px 0 0;padding:14px;background:var(--soft)"><div class="label">当前工作区身份</div><pre class="json" style="margin-top:8px;max-height:220px">'+esc(JSON.stringify({userId:d?.userId||s.identity?.userId||'default',agents:d?.agents||[],sourceAdapters:d?.sourceAdapters||[],projectId:s.identity?.projectId||'unknown',memoryScope:s.identity?.memoryScope||'project'},null,2))+'</pre></div></section>'}
function importPage(){return header('导入','通过受控导入 API 引入其他 AI 的长期记忆')+'<section class="panel"><div class="notice">浏览器不会直接写入 SQLite。当前 Gateway 版本提供只读审计界面；导入请从 DSH 插件的“导入别的 AI 记忆”入口发起，完成预览、冲突判断和撤销。</div><p class="muted">导入协议：stratagate.external-memory.v2 · 所有候选必须经过人工确认或模型决策，并保留来源 Block。</p><button class="btn primary" data-action="copy-prompt">复制导入协议说明</button></section>'}
function sourceMessages(ids){const set=new Set(ids), list=messages().filter(m=>set.has(m.id));return list.length?list.map(m=>'<div class="message"><b>'+esc(m.role)+' · '+esc(fmt(m.createdAt))+'</b>'+esc(m.content)+'</div>').join(''):'<div class="empty">未找到来源消息</div>'}
function modal(){if(!state.selected)return'';const x=state.selected,s=state.snapshot||{};let title=x.title||x.name||x.id, body='';if(x.kind==='raw'){const rows=threadRows().find(v=>v.id===x.id)?.messages||messages().filter(v=>v.id===x.id);body='<p class="muted">原始消息只读展示，内容来自当前 namespace 的 L5/open tail。</p>'+rows.map(m=>'<div class="message"><b>'+esc(m.role)+' · '+esc(fmt(m.createdAt))+'</b>'+esc(m.content)+'</div>').join('')}else if(x.kind==='event'){const e=(s.events||[]).find(v=>v.id===x.id)||{};body='<p>'+esc(e.summary||e.narrative||'暂无摘要')+'</p><h4>时间与治理</h4><pre class="json">'+esc(JSON.stringify({temporal:e.temporal,confidence:e.confidence,status:e.status,weight:e.weight},null,2))+'</pre><h4>来源消息</h4>'+sourceMessages(e.sourceMessageIds||[])}else if(x.kind==='element'){const e=(s.elements||[]).find(v=>v.id===x.id)||{};body='<p>'+esc(e.currentState||'暂无当前状态')+'</p><pre class="json">'+esc(JSON.stringify({type:e.type,aliases:e.aliases,facts:e.facts,sourceEventIds:e.sourceEventIds},null,2))+'</pre><h4>来源消息</h4>'+sourceMessages(e.sourceMessageIds||[])}else{const n=(s.graphNodes||[]).find(v=>v.id===x.id)||{};body='<p>'+esc(n.currentState||'暂无当前状态')+'</p><pre class="json">'+esc(JSON.stringify({type:n.type,aliases:n.aliases,facts:n.facts,sourceEventIds:n.sourceEventIds},null,2))+'</pre><h4>来源消息</h4>'+sourceMessages((n.sourceEventIds||[]).flatMap(id=>(s.events||[]).find(e=>e.id===id)?.sourceMessageIds||[]))}return'<div class="modal" data-action="close-modal"><section class="dialog"><div class="dialog-head"><div><div class="label">'+esc(kindName[x.kind]||'来源详情')+'</div><h3>'+esc(title)+'</h3></div><button class="close" data-action="close-modal">×</button></div><div class="source">'+body+'</div></section></div>'}
function render(){if(state.authenticated===false){showLogin();return}const root=document.getElementById('app'), rows=state.dashboard?.namespaces||[];const nav=[['overview','▣','记忆总览'],['conversations','⌁','对话来源'],['blocks','◇','Block 分层'],['structure','≡','记忆结构'],['processing','↻','处理状态'],['audit','↗','使用审计'],['import','⇄','导入'],['settings','⚙','设置']];let content=state.view==='overview'?overview():state.view==='conversations'?conversations():state.view==='blocks'?blocks():state.view==='structure'?structure():state.view==='processing'?processing():state.view==='audit'?audit():state.view==='import'?importPage():settings();root.innerHTML='<div class="app"><header class="top"><div class="brand"><div class="logo">✦</div><div><h1>StrataGate</h1><small>MEMORY CONSOLE</small></div></div><nav class="nav"><button class="active">记忆控制台</button><span class="muted">只读审计视图</span></nav></header><div class="layout"><aside class="side">'+nav.map(x=>'<button class="'+(state.view===x[0]?'active':'')+'" data-view="'+x[0]+'"><span class="ico">'+x[1]+'</span>'+x[2]+'</button>').join('<div class="side-sep"></div>')+'</aside><main class="content">'+(state.error?'<div class="error">暂时无法读取完整记忆：'+esc(state.error)+'</div>':'')+(state.loading&&!state.dashboard?'<section class="panel"><div class="empty">正在连接 Memory Gateway…</div></section>':content)+'</main></div><div class="footer">SQLite 是 Gateway 的存储层 · 浏览器没有 SQL 权限 · 源消息始终保留</div></div>'+modal();bind()}
function bind(){document.querySelectorAll('[data-view]').forEach(b=>b.onclick=()=>{state.view=b.dataset.view;state.query='';state.selected=null;render()});const nsWrap=document.getElementById('ns-wrap'),nsTrigger=document.getElementById('ns-trigger');if(nsWrap&&nsTrigger&&!nsTrigger.disabled){nsTrigger.onclick=e=>{e.stopPropagation();state.nsOpen=!state.nsOpen;nsWrap.classList.toggle('open',state.nsOpen);nsTrigger.setAttribute('aria-expanded',String(state.nsOpen));if(state.nsOpen)nsWrap.querySelector('.ns-option.active')?.scrollIntoView({block:'nearest'})};nsWrap.querySelectorAll('[data-ns]').forEach(b=>b.onclick=async e=>{e.stopPropagation();const ns=b.dataset.ns||'';state.nsOpen=false;if(!ns||ns===state.namespace){render();return}state.namespace=ns;state.selected=null;await loadSnapshot();render()})}if(!window.__nsGlobalBound){window.__nsGlobalBound=true;document.addEventListener('click',()=>{const w=document.getElementById('ns-wrap');if(w?.classList.contains('open')){state.nsOpen=false;w.classList.remove('open');w.querySelector('.ns-trigger')?.setAttribute('aria-expanded','false')}});document.addEventListener('keydown',e=>{const w=document.getElementById('ns-wrap');if(!w?.classList.contains('open'))return;if(e.key==='Escape'){state.nsOpen=false;w.classList.remove('open');w.querySelector('.ns-trigger')?.setAttribute('aria-expanded','false')}else if(e.key==='ArrowDown'||e.key==='ArrowUp'){e.preventDefault();const opts=[...w.querySelectorAll('.ns-option')];if(!opts.length)return;const dir=e.key==='ArrowDown'?1:-1;let i=opts.indexOf(document.activeElement);i=i<0?(dir>0?-1:opts.length):i;opts[(i+dir+opts.length)%opts.length]?.focus()}})}document.querySelectorAll('[data-subview]').forEach(b=>b.onclick=()=>{state.subview=b.dataset.subview;state.query='';render()});document.querySelectorAll('[data-thread]').forEach(b=>b.onclick=()=>{const id=b.dataset.thread;state.selected={kind:'raw',id,title:id};render()});document.querySelectorAll('[data-kind]').forEach(b=>b.onclick=()=>{state.selected={kind:b.dataset.kind,id:b.dataset.id,title:b.querySelector('.event-title,.row-title,.graph-node strong')?.textContent||b.dataset.id};render()});document.querySelectorAll('[data-action="refresh"]').forEach(b=>b.onclick=refresh);document.querySelectorAll('[data-action="search"]').forEach(b=>b.onclick=()=>{state.query=document.getElementById('query')?.value||'';render()});document.querySelectorAll('[data-action="close-modal"]').forEach(b=>b.onclick=e=>{if(e.target===b||b.classList.contains('close')){state.selected=null;render()}});document.querySelectorAll('[data-action="copy-prompt"]').forEach(b=>b.onclick=()=>navigator.clipboard?.writeText('stratagate.external-memory.v2：候选记忆必须包含 title、summary、tags、temporal、confidence 和 sourceType，并经过预览与人工确认。').then(()=>{b.textContent='已复制'}).catch(()=>{b.textContent='请手动复制协议名'}))}
async function bootstrap(){const stored=localStorage.getItem('stratagate_gateway_token')||'';if(stored){await loadDashboard();return}const res=await fetch('/health',{cache:'no-store'});if(res.status===401){state.authenticated=false;showLogin();return}if(!res.ok)throw new Error('Gateway 不可用（HTTP '+res.status+'）');state.authenticated=true;await loadDashboard()}
document.addEventListener('change',event=>{const target=event.target;if(!(target instanceof HTMLSelectElement))return;if(target.id==='agent-filter'){state.agentFilter=target.value;render()}else if(target.id==='source-filter'){state.sourceFilter=target.value;render()}});document.addEventListener('click',event=>{const target=event.target;if(target instanceof Element&&target.closest('[data-ns]')){state.agentFilter='';state.sourceFilter=''}});
bootstrap().catch(e=>{state.authenticated=false;state.error=e.message||String(e);showLogin()});
</script></body></html>`
